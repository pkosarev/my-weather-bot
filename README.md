# 🚴‍♂️ Вело-погодный Бот (my-weather-bot)

Это персональный Telegram-бот, написанный на Go, который помогает решить, стоит ли идти кататься на велосипеде. Он не просто показывает погоду, а **анализирует прогноз с помощью LLM (Groq)**, чтобы дать дружелюбный и осмысленный совет.

Проект полностью **контейнеризован** с помощью Docker и Docker Compose.

## 🚀 Основной функционал

* 🤖 **Анализ от LLM:** Все ответы о погоде анализируются нейросетью, чтобы предоставить "живой" совет, а не "сухие" цифры.
* 🛡️ **Поддержка Прокси:** Встроенная поддержка SOCKS5/HTTP прокси для работы с LLM из регионов с геоблокировкой (например, РФ).
* 🌤️ **Проверка погоды:** Команды `/checkride` (на сегодня) и `/forecast` (на 4 дня).
* 💾 **Умное сохранение:** Бот запоминает геолокацию пользователя в базе данных PostgreSQL, чтобы не просить её каждый раз.
* 🔄 **Обновление "на лету":** Отправка геолокации *без* команды просто обновляет данные и выдает свежий прогноз.

## 🛠️ Технологии

* **Язык:** [Go](https://go.dev/) (Golang) 1.22+
* **Архитектура:** Clean Architecture / Modular Monolith
* **Контейнеризация:** [Docker](https://www.docker.com/) & [Docker Compose](https://docs.docker.com/compose/)
* 🤖 **LLM:** [Groq API](https://groq.com/) (Llama 3 / Mixtral)
* **API Погоды:** [OpenWeatherMap](https://openweathermap.org/)
* **База данных:** [PostgreSQL](https://www.postgresql.org/) + драйвер [pgx/v5](https://github.com/jackc/pgx)

## 📁 Структура проекта

Код разбит на независимые пакеты для лучшей читаемости и поддержки:

* `/main.go`: Точка входа. Инициализация зависимостей и запуск.
* `/config/`: Управление конфигурацией и переменными окружения.
* `/bot/`: Бизнес-логика бота, роутинг команд и управление состоянием (`userState`).
* `/weather/`: Работа с API OpenWeatherMap (получение данных и подготовка сводок).
* `/llm/`: Клиент для работы с нейросетями (включая настройку прокси).
* `/storage/`: Слой работы с базой данных PostgreSQL.
* `docker-compose.yml`: Оркестрация контейнеров приложения и БД.

---

## ⚙️ Запуск (Локально через Docker)

Вам **не нужен** установленный Go или PostgreSQL. Нужен только **Docker Desktop**.

### 1. Клонирование репозитория

```bash
git clone [https://github.com/pkosarev/my-weather-bot.git](https://github.com/pkosarev/my-weather-bot.git)
cd my-weather-bot
```

### 2. Настройка окружения (.env)

Создайте файл `.env` на основе примера:

```bash
cp .env.example .env
```

Откройте `.env` и заполните ключи:

```bash
# Токен от @BotFather в Telegram
TELEGRAM_TOKEN=123456:ABC...

# Ваш API-ключ с сайта OpenWeatherMap
WEATHER_API_KEY=abc123...

# Ваш API-ключ с сайта Groq
GROQ_API_KEY=gsk_...

# (Опционально) SOCKS5 Прокси для обхода блокировок Groq
# Формат: socks5://user:pass@host:port
PROXY_URL=socks5://myuser:mypass@192.168.1.1:1080

# Настройки БД (можно оставить по умолчанию для Docker)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=mysecretpassword
POSTGRES_DB=my-weather-bot-db
```

### 3. Сборка и Запуск

Выполните одну команду в терминале:

```bash
docker compose up -d --build
```
*(Флаг `--build` пересобирает проект при изменении кода).*

### 4. Полезные команды

Смотреть логи бота (в реальном времени):

```bash
docker compose logs -f bot
```

Остановить бота:

```bash
docker compose down
```

---

### 💎 Особенности реализации
* Сетевая устойчивость: HTTP-клиент настроен с жесткими таймаутами и отключенным KeepAlive, чтобы избежать зависаний соединения (Long Polling) при нестабильной сети.

* Multi-stage Build: Docker-образ собирается в два этапа, итоговый контейнер (на базе Alpine Linux) весит минимум места.

* Отказоустойчивость LLM: Если нейросеть недоступна или ключ неверный, бот автоматически переключается на "запасной вариант" и выдает сухую сводку погоды, не ломаясь.
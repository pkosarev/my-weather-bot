# 🚴‍♂️ Вело-погодный Бот (my-weather-bot)

Это персональный Telegram-бот, написанный на Go, который помогает решить, стоит ли идти кататься на велосипеде. Он проверяет погоду (текущую и прогноз) и хранит геолокацию пользователя для удобства.

## 🚀 Основной функционал

* **Проверка погоды на сегодня:** Команда `/checkride` запрашивает геолокацию, сообщает о погоде *на сейчас* и **сохраняет** эту геолокацию для будущих запросов.
* **Прогноз на 4 дня:** Команда `/forecast` немедленно выдает прогноз на 4 дня (пропуская сегодня), используя **ранее сохраненную** геолокацию.
* **Умное сохранение:** Если геолокация не сохранена, `/forecast` попросит ее один раз. Любая отправленная геолокация (даже без команды) обновляет сохраненную и выдает прогноз.
* **Сброс геолокации:** Команды `/start` и `/forgetlocation` удаляют сохраненные данные о геолокации пользователя.

## 🛠️ Технологии

* **Язык:** [Go](https://go.dev/) (Golang)
* **API Погоды:** [OpenWeatherMap](https://openweathermap.org/) (API 5-дневного прогноза)
* **Telegram API:** [go-telegram-bot-api/v5](https://github.com/go-telegram-bot-api/telegram-bot-api)
* **База данных:** [PostgreSQL](https://www.postgresql.org/) (для хранения геолокаций пользователей)
* **Драйвер БД:** [pgx/v5](https://github.com/jackc/pgx)
* **Хостинг (Деплой):** [Render](https://render.com/) (PaaS)

## 📁 Структура проекта

Код разбит на пакеты для лучшей читаемости и поддержки (Separation of Concerns):

* `/main.go`: Точка входа, логика Telegram-бота, обработка команд и управление "состояниями" (`userState`).
* `/weather/weather.go`: Вся логика, связанная с API OpenWeatherMap (запросы, парсинг JSON, анализ прогноза).
* `/storage/storage.go`: Вся логика работы с базой данных PostgreSQL (подключение, создание таблицы, `Get`, `Save`, `Delete` геолокации).

---

## ⚙️ Запуск (Локально для разработки)

### 1. Предварительные требования

* Установленный [Go](https://go.dev/doc/install) (версия 1.21+).
* Доступ к базе данных PostgreSQL (можно [запустить локально через Docker](https://www.docker.com/products/docker-desktop/) или использовать "External" URL от Render).

### 2. Клонирование репозитория

```bash
git clone [https://github.com/ВАШ_ЛОГИН/ВАШ_РЕПОЗИТОРИЙ.git](https://github.com/ВАШ_ЛОГИН/ВАШ_РЕПОЗИТОРИЙ.git)
cd my-weather-bot
```

### 3. Установка зависимостей

Go сделает это автоматически при первой сборке, но для "очистки" go.mod выполните:

```bash
go mod tidy
```

### 4. Файл .env

Создайте файл .env в корне проекта и заполните его тремя ключами:

```bash
# Токен от @BotFather в Telegram
TELEGRAM_TOKEN=123456:ABC...

# Ваш API-ключ с сайта OpenWeatherMap
WEATHER_API_KEY=abc123...

# Строка подключения к вашей ЛОКАЛЬНОЙ или ТЕСТОВОЙ базе данных
# (Например, "External Connection String" от базы на Render)
DATABASE_URL=postgres://USER:PASSWORD@HOST/DATABASE
```

### 5. Запуск

```bash
go run main.go
```

Бот запустится и подключится к вашей базе данных.